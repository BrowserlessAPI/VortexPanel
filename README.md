# VortexPanel

Server control panel — Python + FastAPI + Alpine.js.

## Install

```bash
git clone https://github.com/BrowserlessAPI/Vortexpanel.git
cd Vortexpanel
sudo bash install.sh
```

## Development

```bash
pip install -r requirements.txt
python run.py --reload
```
